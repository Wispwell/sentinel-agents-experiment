# secrets
.env
.env.local

# experiment output — run logs (runs/) are now committed as evidence
logs/
figures/

# board substrate (local, non-container mode)
board_local/

# target state — rebuilt per run, never committed
target/state/
target/**/access.log
target/**/gateway.lock

# python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.ruff_cache/
.pytest_cache/
.mypy_cache/

# editors / os
.DS_Store
.idea/
.vscode/
cache_state/
service_state/
gatedrop_state/

# Local Claude Code settings (per-machine, not project code)
.claude/

# generated results pages (reproducible via results.py from runs/)
results/
